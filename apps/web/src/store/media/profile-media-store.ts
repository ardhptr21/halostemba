import { createMediaStore, createPreviewMediaStore } from "./media-store";

export const useMediaStoreProfile = createMediaStore();
export const usePreviewMediaStoreProfile = createPreviewMediaStore();
