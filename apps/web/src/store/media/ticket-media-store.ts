import { createMediaStore, createPreviewMediaStore } from "./media-store";

export const useMediaStoreTicket = createMediaStore();
export const usePreviewMediaStoreTicket = createPreviewMediaStore();
