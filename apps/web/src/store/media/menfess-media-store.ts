import { createMediaStore, createPreviewMediaStore } from "./media-store";

export const useMediaStoreMenfess = createMediaStore();
export const usePreviewMediaStoreMenfess = createPreviewMediaStore();
